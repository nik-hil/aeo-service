# Leadership UI — visual QA notes

Generated by `scripts`/runtime Playwright pass against http://127.0.0.1:7860/.

## Viewports exercised
- 1366×768
- 1440×900
- 1920×1080

## Flows
- Landing
- One-click demo — single page
- Guide accordion open
- One-click demo — multi-page (same-host crawl seed)
- Detail tabs: Recommendations / After / Evidence

## Observations
- Brand (AEO Leadership Demo) is hero-level in the first viewport.
- Teal/slate B2B theme with Fraunces + Source Sans 3; soft grid atmosphere.
- Demo completes via secured API (`demo_mode`) without UI-side URL fetch.
- Draft copy uses skeleton / generated labeling (not final optimized page).
- Desktop layouts: content readable at 1366; comfortable at 1440/1920.
- Known Gradio quirk: Dataframe `.select` arity — opportunities open via dedicated dropdown instead.
- Demo fixtures always crawl the full fixture site (API ignores max_pages); UI notes this in overview when demo_mode=true.
- Analyze uses primary button variant; demo/clear/guide are secondary.

## Defects found / fixed during verification
- Gradio 5.50: `theme`/`css` remain on `Blocks(...)` (not `launch` yet).
- Local folder `ui/gradio` must not shadow the Gradio package (conftest guard).
- Generator handlers must use `yield from` (lambdas break streaming).

## Extra notes
- None
