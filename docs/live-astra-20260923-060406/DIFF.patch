--- CURRENT.md
+++ RECOMMENDED.md
@@ -280,7 +280,7 @@
 finish(...)
 ```
 
-The harness then knows it can terminate the loop.
+The function itself only returns data; the harness must recognize the `finish` call and terminate the loop. The high-level loop earlier does not show this finish-specific branch and also stops when there are no tool calls. Exposing `finish` alone therefore does not make it the only stopping condition.
 
 This becomes very useful as the agent starts performing multi-step work.
 
@@ -411,6 +411,8 @@
     }
 )
 ```
+
+The `tool_call_id` must match the ID of the originating assistant tool call. It links this result to that particular request, even when the model calls the same tool more than once.
 
 Now the model can observe what happened.
 
@@ -466,6 +468,8 @@
 ```
 
 The message history is therefore a critical part of the harness.
+
+In the implementation, append the complete assistant message containing the tool calls—their IDs, names, and arguments—before appending the corresponding tool results. Keeping the attempted code in those arguments alongside its returned error lets the next model turn revise the failed attempt rather than see an error without knowing what produced it.
 
 Later, this will evolve into a much larger problem:
 
@@ -543,6 +547,8 @@
 ```
 
 At this stage there are essentially no meaningful security boundaries.
+
+A subprocess is not a sandbox. The 10-second timeout does not restrict what the generated Python can access or prevent it from causing damage before the timeout expires.
 
 That is intentional.
 
@@ -696,4 +702,4 @@
 
 It is to understand what the frameworks are doing for us.
 
-One Git tag at a time.
+One Git tag at a time.