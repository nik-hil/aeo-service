--- CURRENT.md
+++ RECOMMENDED.md
@@ -1,3 +1,4 @@
+---
 # Agents Zero to Hero #1: Building an AI Agent from Scratch with Tool Calling
 
 Building an AI agent sounds deceptively simple.
@@ -7,6 +8,8 @@
 But that is not really an agent.
 
 An LLM becomes an agent when it can **decide to take actions**, invoke capabilities outside the model, observe the result, and continue working until the task is complete.
+
+A prompt usually produces a response. An agent combines the LLM with tools, execution, state, and a control loop so it can act, observe, and choose what to do next.
 
 That sounds complicated when described using terms like *agentic workflows*, *tool calling*, *harnesses*, and *autonomous coding agents*.
 
@@ -61,7 +64,7 @@
 
 The most important concept in the entire project is the agent loop.
 
-At a high level:
+At a high level, the loop sends the task and tool schemas to the LLM, executes any requested tool, adds the result to the conversation, and repeats until the model is finished.
 
 ```python
 while True:
@@ -168,6 +171,8 @@
 
 This schema is effectively the contract between the model and the harness.
 
+It describes the capability to the model, but it does not contain the Python implementation or automatically execute anything. The harness still has to parse the model's structured call and dispatch it to the registered function.
+
 The model sees:
 
 ```text
@@ -267,6 +272,8 @@
 Why make finishing a tool instead of simply accepting a normal assistant response?
 
 Because it gives the harness an explicit completion signal.
+
+In this minimal loop, a normal assistant response also causes the loop to break because it contains no tool calls. The `finish` tool makes completion an explicit, structured event instead of relying only on the absence of a tool call. That becomes more useful when the harness needs to validate completion or add richer completion behavior.
 
 The model can say:
 
@@ -464,6 +471,8 @@
       ↓
 ...
 ```
+
+The updated message history is sent with the next model call. Because each tool result becomes part of that history, the model can inspect the result and decide whether to retry, call another tool, or finish.
 
 The message history is therefore a critical part of the harness.
 
@@ -542,6 +551,8 @@
 )
 ```
 
+That means model-supplied code is executed by a subprocess with the access and privileges available to the agent process. The tool schema describes the capability, but it is not a security boundary.
+
 At this stage there are essentially no meaningful security boundaries.
 
 That is intentional.
@@ -569,6 +580,8 @@
 The first lesson is surprisingly simple:
 
 An agent is not just a prompt.
+
+The model proposes actions, but the harness owns tool execution, state, permissions, observability, error handling, and policy enforcement.
 
 The minimal architecture is:
 
@@ -614,6 +627,8 @@
 
 # Running the project
 
+This runs the repository's first basic tool-calling milestone, `v0.1-basic-tool`.
+
 Clone the repository:
 
 ```bash
@@ -697,3 +712,5 @@
 It is to understand what the frameworks are doing for us.
 
 One Git tag at a time.
+
+---