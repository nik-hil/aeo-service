--- CURRENT.md
+++ RECOMMENDED.md
@@ -280,7 +280,7 @@
 finish(...)
 ```
 
-The harness then knows it can terminate the loop.
+The high-level loop above stops when a response has no tool calls; it does not show this explicit `finish` handling. Here, `finish` returns the final answer, and the harness must recognize that completion signal to terminate the loop.
 
 This becomes very useful as the agent starts performing multi-step work.
 
@@ -411,6 +411,8 @@
     }
 )
 ```
+
+`tool_call_id` links this result to the specific tool call that requested it, so results remain associated with their requests even when the model makes multiple calls.
 
 Now the model can observe what happened.
 
@@ -465,7 +467,7 @@
 ...
 ```
 
-The message history is therefore a critical part of the harness.
+The message history must therefore retain the assistant's tool call followed by its corresponding tool result—not just the result. The opening loop is a high-level sketch: it shows appending results but leaves out preserving the assistant tool-call message.
 
 Later, this will evolve into a much larger problem:
 
@@ -696,4 +698,4 @@
 
 It is to understand what the frameworks are doing for us.
 
-One Git tag at a time.
+One Git tag at a time.