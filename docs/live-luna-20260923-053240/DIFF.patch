--- CURRENT.md
+++ RECOMMENDED.md
@@ -412,6 +412,8 @@
 )
 ```
 
+The `tool_call_id` must match the ID from the original tool request so the model and harness can bind this result to the specific call that produced it.
+
 Now the model can observe what happened.
 
 For example:
@@ -444,6 +446,8 @@
 # Why the message history matters
 
 The model does not remember tool executions automatically.
+
+The harness must preserve the assistant message containing the tool call, append the matching tool result after it, and send the complete sequence back to the model on the next iteration.
 
 The harness maintains the conversation state:
 
@@ -696,4 +700,4 @@
 
 It is to understand what the frameworks are doing for us.
 
-One Git tag at a time.
+One Git tag at a time.